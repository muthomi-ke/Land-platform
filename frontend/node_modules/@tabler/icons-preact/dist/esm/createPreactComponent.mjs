/**
 * @license @tabler/icons-preact v3.36.0 - MIT
 *
 * This source code is licensed under the MIT license.
 * See the LICENSE file in the root directory of this source tree.
 */

import { h, toChildArray } from 'preact';
import defaultAttributes from './defaultAttributes.mjs';

const createPreactComponent = (type, iconName, iconNamePascal, iconNode) => {
  const Component = ({
    color = "currentColor",
    size = 24,
    stroke = 2,
    title,
    children,
    className = "",
    class: classes = "",
    style,
    ...rest
  }) => h(
    "svg",
    {
      ...defaultAttributes[type],
      width: String(size),
      height: String(size),
      class: [`tabler-icon`, `tabler-icon-${iconName}`, classes, className].join(" "),
      ...type === "filled" ? {
        fill: color
      } : {
        "stroke-width": stroke,
        stroke: color
      },
      style,
      ...rest
    },
    [
      title && h("title", {}, title),
      ...iconNode.map(([tag, attrs]) => h(tag, attrs)),
      ...toChildArray(children)
    ]
  );
  Component.displayName = `${iconNamePascal}`;
  return Component;
};

export { createPreactComponent as default };
//# sourceMappingURL=createPreactComponent.mjs.map
